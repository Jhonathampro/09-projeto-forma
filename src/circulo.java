public class circulo extends Forma {
    public circulo(int cordx, int cordy, double raio) {
        super(cordx, cordy, raio);
    }

    @Override
    public double calcularArea() {
      //  double area;
       // double pi = 3;

      //  area = pi * Math.pow(getRaio(), 2);
      //  return  area;
        return 2;
    }
}
