// tipo de classe que agrupa metodos métodos
// para compartilhar com outras classe.
// interface -> abstract class.
// métodos da interface são métodos abstratos.
// métodos são publicos mesmo que não esteja marcado
// com public.
// interface não pode instaciada porque não tem cosntrutor
public interface Volume {
    public abstract double calcularVolume();
}
